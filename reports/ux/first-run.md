# First-Run UX Evidence

Status: prototype evidence only.

Command verified locally:

```text
python3 -m aegis_challenge.cli run examples/starter_strategy.py --bundle smoke --seed 1
```

Observed output:

```text
run_id = 8a7711c8f5cf0ab1
run_dir = runs/8a7711c8f5cf0ab1
```

The CLI first-run path works, but this is not yet representative participant-trial evidence. Public launch still requires timed participant trials, replay comprehension checks, accessibility checks, and screenshot review.
